export enum TeamType {
    FRONTEND = "frontend",
    BACKEND = "backend",
    TESTING = "testing",
}

export enum RoleTypes {
    Admin = 'admin',
    User = 'user'
}

export enum StatusTypes {
    Pending = 'pending',
    Accepted = 'accepted',
    Rejected = 'rejected'
}