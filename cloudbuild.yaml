steps:
- name: 'gcr.io/cloud-builders/npm'
  args: ['install']
- name: 'gcr.io/cloud-builders/npm'
  args: ['start']
options:
  logging: CLOUD_LOGGING_ONLY  # Configures logging for Cloud Logging only